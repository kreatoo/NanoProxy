{
  "name": "opencode-nanogpt-tool-bridge",
  "private": false,
  "version": "1.0.0",
  "description": "Local OpenAI-compatible proxy that bridges OpenCode tool calls for NanoGPT when native tool calling is unreliable.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node selftest.js"
  }
}
